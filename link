#!/bin/bash

link() {
    rm -rf ~/$1
    ln -s $(pwd)/$1 ~/$1
}

mkdir -p ~/bin

link .vimrc
link .vim
link .bashrc
link .tmux.conf
link .profile
link .tmux
rm -f ~/bin/myip

vim +PluginInstall +qall
~/.tmux/plugins/tpm/bin/install_plugins
